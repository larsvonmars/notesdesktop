# Worker secrets (set via Cloudflare dashboard/CLI in production)
DEEPSEEK_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional CORS lock-down (only needed if API is on a different origin)
ALLOWED_ORIGIN=

# Optional overrides
AI_RATE_LIMIT_REQUESTS=40
AI_RATE_LIMIT_WINDOW_MS=60000
AI_UPSTREAM_TIMEOUT_MS=30000
